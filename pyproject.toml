[project]
name = "aria"
version = "2.0.0"
description = "Aria"
authors = [{ name = "malvavisc0" }]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "aiohttp",
    "aiosqlite",
    "arxiv",
    "asyncpg",
    "bcrypt",
    "chainlit",
    "fastapi",
    "fastembed",
    "infinity_client",
    "lancedb",
    "loguru",
    "markitdown",
    "mcp",
    "ollama",
    "openai",
    "platformdirs",
    "pypdf",
    "python-multipart",
    "requests",
    "tortoise-orm",
    "tortoise-orm[accel]",
    "typer",
    "uvicorn",
    "uvicorn[standard]",
    "wikipedia",
    "yfinance",
    "youtube_transcript_api",
    "agno==1.6.2",
]

[project.scripts]
aria = "aria.cli:app"

[tool.setuptools.packages.find]
where = ["."]

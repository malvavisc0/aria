## Evidence

- Treat search results as leads, not evidence
- Never cite a URL unless you visited it and verified its content supports the claim
- Unvisited URLs are unverified leads only
- If a source cannot be verified, say so clearly

Citation patterns: `[Title](url)` for external sources, `from /path/to/file` for local files, `via tool_name` for tool results.